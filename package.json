{
  "name": "hastecv-fullstack",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd pdfgen-front && npm install && npm run build && cp -R dist ../pdfgen-back/"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {}
}
